<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <com.google.android.material.textview.MaterialTextView
        android:id="@+id/title"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_gravity="center_horizontal|center_vertical"
        android:layout_marginBottom="@dimen/splash_text_margin_bottom"
        android:alpha="@dimen/splash_start_alpha"
        android:ellipsize="end"
        android:fontFamily="@font/caveat_regular"
        android:gravity="bottom|center_horizontal"
        android:scaleX="@dimen/splash_scale_start"
        android:scaleY="@dimen/splash_scale_start"
        android:text="@string/app_name"
        android:textColor="@color/white"
        android:textSize="@dimen/splash_text_size" />

    <androidx.appcompat.widget.AppCompatImageView
        android:id="@+id/splash_image"
        android:layout_width="@dimen/splash_image_size"
        android:layout_height="@dimen/splash_image_size"
        android:layout_gravity="bottom|center"
        android:layout_margin="@dimen/splash_image_margin"
        android:alpha="@dimen/splash_start_alpha"
        android:scaleX="@dimen/splash_scale_start"
        android:scaleY="@dimen/splash_scale_start"
        app:srcCompat="@drawable/img_camera" />
</FrameLayout>
